<%= form_with(model: phycision, local: true) do |form| %>
  <% if phycision.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(phycision.errors.count, "error") %> prohibited this phycision from being saved:</h2>

      <ul>
        <% phycision.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
